<dashboard theme="dark">
  <label>Content Inventory</label>
  <description>Please install the Apps: ES, ESCU, Security Essentials</description>
  <row>
    <panel>
      <single>
        <title>All</title>
        <search>
          <query>| rest splunk_server=local count=0 /services/saved/searches 
| search action.correlationsearch.enabled = "1"
| search title!="ESCU - *"
| rename eai:acl.app as app, action.correlationsearch.label as title, action.notable.param.security_domain as security_domain 
| rex field=qualifiedSearch ".*datamodel(=\"|=|:\"|\(\"|\s|:)(?&lt;datamodel&gt;[a-z,A-Z,_]*)"
| eval source="ES"
| sort title

| append [
| sseanalytics
| eval source="Security Essentials" 
| eval hasSearch="Yes"
| rename datasource as providing_technologies domain as security_domain mitre_technique_display as mitre usecase as use_case
| table source, app, title, description, datamodel, providing_technologies, security_domain, category, use_case, mitre, killchain
| sort search
]

| append [
| sseanalytics   | eval hasSearch=spath(summaries, "hasSearch") | eval includeSSE=spath(summaries, "includeSSE") | eval datasource=spath(summaries, "datasource") | eval displayapp=spath(summaries, "displayapp") | eval app=spath(summaries, "app") | eval journey=spath(summaries, "journey") | eval category=spath(summaries, "category") | eval domain=spath(summaries, "domain") | eval name=spath(summaries, "name") | eval icon=spath(summaries, "icon") | eval description=spath(summaries, "description") | eval dashboard=spath(summaries, "dashboard") | eval mitre=spath(summaries, "mitre") | eval killchain=spath(summaries, "killchain") | eval alertvolume=spath(summaries, "alertvolume") | eval highlight=spath(summaries, "highlight") | eval SPLEase=spath(summaries, "SPLEase")| eval includeSSE=spath(summaries, "includeSSE") | eval bookmark_status=spath(summaries, "bookmark_status")| eval bookmark_user=spath(summaries, "bookmark_user")  | eval usecase=spath(summaries, "usecase") | eval released=spath(summaries, "released") 
| eval pipefields = null 
| foreach category datasourc* killchain mitre usecase 
    [| makemv &lt;&lt;FIELD&gt;&gt; delim="|" ] 
| search displayapp= "Splunk User Behavior Analytics"
| rename name as title, usecase as use_case, datasource as providing_technologies, domain as security_domain
| nomv mitre | nomv killchain | nomv category | nomv providing_technologies | nomv use_case
| eval mitre = replace(mitre, "\n", " | ") | eval killchain = replace(killchain, "\n", " | ") | eval category = replace(category, "\n", " | ") | eval use_case = replace(use_case, "\n", " | ")
| eval providing_technologies = replace(providing_technologies, ",", " |")
| eval description = replace(description, ",", "")
| eval source="UBA"
| sort title
]

| append [
| rest splunk_server=local count=0 /services/saved/searches 
| search action.correlationsearch.enabled = "1"
| search title="ESCU - *"
| rename eai:acl.app as app, action.correlationsearch.label as title, action.notable.param.security_domain as security_domain 
| rex field=qualifiedSearch ".*datamodel(=\"|=|:\"|\(\"|\s|:)(?&lt;datamodel&gt;[a-z,A-Z,_]*)"
| eval description = replace(description, ",", "")
| eval source="ESCU Searches"
| sort title
]

| append [
| rest /services/configs/conf-analytic_stories splunk_server=local count=0
| eval datamodels = replace(data_models, "[\[\]\"]", "") 
| eval providing_technologies = replace(providing_technologies, "[\[\]\"]", "")
| eval detection_searches = replace(detection_searches, "[\[\]\"]", "") 
| eval support_searches = replace(support_searches, "[\[\]\"]", "") 
| eval investigative_searches = replace(investigative_searches, "[\[\]\"]", "") 
| eval contextual_searches = replace(contextual_searches, "[\[\]\"]", "") 
| eval providing_technologies = replace(providing_technologies, ",", " |")
| eval description = replace(description, ",", "")
| eval detection_searches = replace(detection_searches, ",", " |")
| eval support_searches = replace(support_searches, ",", " |")
| eval investigative_searches = replace(investigative_searches, ",", " |")
| eval contextual_searches = replace(contextual_searches, ",", " |")
| eval datamodels = replace(datamodels, ",", " |")
| spath input=mappings path=kill_chain_phases{} output="killchain"
| spath input=mappings path=mitre_attack{} output="mitre"
| nomv mitre | nomv killchain
| eval mitre = replace(mitre, "\n", " | ")
| eval killchain = replace(killchain, "\n", " | ")
| rename eai:acl.app as app
| rename datamodels as datamodel
| eval source="ESCU Analytic Stories"
| sort title
]

| table source, app, title, description, datamodel, providing_technologies, security_domain, category, use_case, mitre, killchain, detection_searches, investigative_searches, contextual_searches, support_searches, qualifiedSearch
| sort source
| outputlookup dsa_all.csv
| stats count</query>
          <earliest>-24h@h</earliest>
          <latest>now</latest>
        </search>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
      </single>
    </panel>
  </row>
  <row>
    <panel>
      <single>
        <title>ES Correlation Searches</title>
        <search>
          <query>| rest splunk_server=local count=0 /services/saved/searches 
| search action.correlationsearch.enabled = "1"
| search title!="ESCU - *"
| rename eai:acl.app as app, title as bin, action.correlationsearch.label as search, action.notable.param.security_domain as security_domain 
| rex field=qualifiedSearch ".*datamodel(=\"|=|:\"|\(\"|\s|:)(?&lt;datamodel&gt;[a-z,A-Z,_]*)"
| eval source="ES"
| table source, app, search, description, datamodel, providing_technologies, security_domain, category, use_case, mitre, killchain, qualifiedSearch
| sort search
| outputlookup dsa_es_searches.csv
| stats count</query>
          <earliest>-24h@h</earliest>
          <latest>now</latest>
        </search>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
      </single>
    </panel>
    <panel>
      <single>
        <title>ESCU Analytic Stories</title>
        <search>
          <query>| rest /services/configs/conf-analytic_stories splunk_server=local count=0
| eval datamodels = replace(data_models, "[\[\]\"]", "") 
| eval providing_technologies = replace(providing_technologies, "[\[\]\"]", "")
| eval detection_searches = replace(detection_searches, "[\[\]\"]", "") 
| eval support_searches = replace(support_searches, "[\[\]\"]", "") 
| eval investigative_searches = replace(investigative_searches, "[\[\]\"]", "") 
| eval contextual_searches = replace(contextual_searches, "[\[\]\"]", "") 
| eval providing_technologies = replace(providing_technologies, ",", " |")
| eval description = replace(description, ",", "")
| eval detection_searches = replace(detection_searches, ",", " |")
| eval support_searches = replace(support_searches, ",", " |")
| eval investigative_searches = replace(investigative_searches, ",", " |")
| eval contextual_searches = replace(contextual_searches, ",", " |")
| eval datamodels = replace(datamodels, ",", " |")
| spath input=mappings path=kill_chain_phases{} output="killchain"
| spath input=mappings path=mitre_attack{} output="mitre"
| nomv mitre | nomv killchain
| eval mitre = replace(mitre, "\n", " | ")
| eval killchain = replace(killchain, "\n", " | ")
| rename eai:acl.app as app
| eval source="ESCU Analytic Stories"
| table source, app, title, description, datamodels, providing_technologies, security_domain, category, use_case, mitre, killchain, detection_searches, investigative_searches, contextual_searches, support_searches
| sort title
| outputlookup dsa_escu_analyticStories.csv
| stats count</query>
          <earliest>-24h@h</earliest>
          <latest>now</latest>
        </search>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
      </single>
    </panel>
    <panel>
      <single>
        <title>ESCU Searches</title>
        <search>
          <query>| rest splunk_server=local count=0 /services/saved/searches 
| search action.correlationsearch.enabled = "1"
| search title="ESCU - *"
| rename eai:acl.app as app, title as bin, action.correlationsearch.label as search, action.notable.param.security_domain as security_domain 
| rex field=qualifiedSearch ".*datamodel(=\"|=|:\"|\(\"|\s|:)(?&lt;datamodel&gt;[a-z,A-Z,_]*)"
| eval description = replace(description, ",", "")
| eval source="ESCU Searches"
| table source, app, search, description, datamodel, providing_technologies, security_domain, category, use_case, mitre, killchain, qualifiedSearch
| sort search
| outputlookup dsa_escu_searches.csv
| stats count</query>
          <earliest>-24h@h</earliest>
          <latest>now</latest>
        </search>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
      </single>
    </panel>
    <panel>
      <single>
        <title>Security Essentials</title>
        <search>
          <query>| sseanalytics
| eval source="Security Essentials" 
| search hasSearch="Yes" app="Splunk_Security_Essentials"
| rename datasource as providing_technologies domain as security_domain mitre_technique_display as mitre usecase as use_case
| table source, app, title, description, datamodel, providing_technologies, security_domain, category, use_case, mitre, killchain
| sort search
| outputlookup dsa_sse_searches.csv
| stats count</query>
          <earliest>-24h@h</earliest>
          <latest>now</latest>
        </search>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
      </single>
    </panel>
    <panel>
      <single>
        <title>UBA</title>
        <search>
          <query>| sseanalytics
| eval source="Security Essentials" 
| search app="Splunk_User_Behavior_Analytics"
| rename datasource as providing_technologies domain as security_domain mitre_technique_display as mitre usecase as use_case
| table source, app, title, description, datamodel, providing_technologies, security_domain, category, use_case, mitre, killchain
| sort search
| outputlookup dsa_sse_searches.csv
| stats count</query>
          <earliest>-24h@h</earliest>
          <latest>now</latest>
        </search>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
      </single>
    </panel>
  </row>
</dashboard>